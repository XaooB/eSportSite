<main>
    <div class="container">
        <div class='error_wrapper hidden'>
            <section class='error'>
                <h2>{{title}}</h2>
                <div class="info">
                    <p>{{desc}}</p>
                    <h4>{{fun}}</h4>
                </div>
            </section>
        </div>
    </div>
</main>
